* TODO:

* `type Server struct {}`
  * manages ALL peer connections
  * TODO:
  * `newTransport func(net.Conn, *ecdsa.PublicKey) transport`
    * hooks -- for -- testing
    * uses
      * inhibit the WHOLE protocol stack
  * TODO:

* `type transport interface {}`
  * 2 handshakes
  * TODO:

* TODO: