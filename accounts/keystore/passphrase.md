* == keystore /
  * 's behavior vs `KeyStorePlain`'s behavior
    * private key is encrypted /
      * | disk, uses ANOTHER JSON encoding

* `StandardScryptN`
  * == Scrypt encryption algorithm's `N` parameter /
    * use 256MB memory
    * | modern processor,
      * take approximately 1s CPU time 

* `StandardScryptP`
  * == Scrypt encryption algorithm's `P` parameter /
    * use 256MB memory
    * | modern processor,
        * take approximately 1s CPU time

* TODO:
