* `func initializeSecrets(c *cli.Context) error {}`
  * 's functionality
    * if there is an error | 
      * `initialize()` OR look up "masterseed.json" -> return `error`
    * `masterSeed`
      * `[]byte` / 's length = 256
    * get prompted passPhrase

* TODO:

* `func initialize(c *cli.Context) error {}`
  * set up the logger

* TODO: