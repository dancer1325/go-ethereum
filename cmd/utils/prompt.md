* `func GetPassPhrase(text string, confirmation bool) string {}`
  * 's input
    * `text string`
      * echoed out | terminal
  * 's functionality
    * request textual data / 
      * need to be prompted
      * NOT echoed out | terminal
  * 's return
    * `string`
      * prompted -- by the -- user
